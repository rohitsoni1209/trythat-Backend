export enum PropRelType {
  ORGS = 'orgs',
  OCCUPANTS = 'occupants',
  UNITS = 'units',
}

export enum OrgRelType {
  PROPS = 'props',
  CONNECTS = 'connects',
}

export enum ConnectRelType {
  ORGS = 'orgs',
}
