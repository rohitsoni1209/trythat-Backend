export enum ResourceType {
  PROPERTY = 'property',
  ORGANIZATION = 'organization',
  CONNECT = 'connect',
  UNIT = 'unit',
}

/*
  ATTENTION ATTENTION
  Using this since the recommendation api currently is in beta and the payload keeps changing
  */
export enum _ResourceType {
  PROPERTY = 'Property',
  ORGANIZATION = 'Company',
  CONNECT = 'Individual',
  UNIT = 'Unit',
}
